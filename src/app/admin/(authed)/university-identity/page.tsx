export const metadata = { title: 'University Identity' };

export default function UniversityIdentityPage() {
  return (
    <div>
      <h1 className="text-2xl font-display font-bold text-gray-900">
        University Identity
      </h1>
      <p className="mt-2 text-sm text-gray-500 italic">
        Address, phones, emails, social URLs, copyright, map, footer logo —
        coming in Sub-step 5.2.
      </p>
    </div>
  );
}
