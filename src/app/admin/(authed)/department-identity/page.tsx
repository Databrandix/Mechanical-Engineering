export const metadata = { title: 'Department Identity' };

export default function DepartmentIdentityPage() {
  return (
    <div>
      <h1 className="text-2xl font-display font-bold text-gray-900">
        Department Identity
      </h1>
      <p className="mt-2 text-sm text-gray-500 italic">
        Brand colors, logo, hero images, breadcrumb label, and faculty name
        editing — coming in Sub-step 5.2.
      </p>
    </div>
  );
}
